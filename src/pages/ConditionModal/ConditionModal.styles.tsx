import { styled } from '@mui/material/styles';

export const DialogContainer = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  width: 300,
  height: 526,
  top: 166,
  left: 16,
  borderRadius: 16,
});
